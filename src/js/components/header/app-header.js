/** @jsx React.DOM */
var React = require('react');
var Header =
  React.createClass({
    render:function(){
      return (
        <h2>Hello World</h2>
        )
    }
  });
module.exports = Header;
